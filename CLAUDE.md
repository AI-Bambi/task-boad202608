# CLAUDE.md

## 目的
このファイルは、本プロジェクトにおけるGit運用ルールを明確にするためのガイドです。

## Git運用ルール

1. 変更は小さな単位で行う
   - 1つのコミットは1つの目的に集中させる。
   - 不要なファイルや一時ファイルはコミットしない。

2. コミットメッセージ
   - 簡潔でわかりやすく書く。
   - 例: `fix: ログ表示のバグ修正`、`feat: タスク追加画面のUI改善`。

3. プッシュのタイミング
   - コードを変更したら、必ずGitHubへプッシュする。
   - ローカルでの作業が完了したら、`git add`、`git commit`、`git push` の順に実行する。
   - 途中段階でも作業状態を残したい場合は、適切なコミットを行ってプッシュする。

4. ブランチ運用
   - `main`（または `master`）は常に安定した状態を保つ。
   - 新機能や修正は `feature/`、`fix/` などのブランチを切って作業する。
   - 例: `feature/task-editor`, `fix/login-error`

5. Pull Request（PR）
   - 他のメンバーがいる場合やレビューが必要な場合は、PRを作成する。
   - PRには変更内容の要点と確認してほしい点を明記する。

6. 変更履歴の共有
   - GitHubへのプッシュにより、変更履歴と進捗をチームで共有する。
   - プッシュ漏れがあると最新の作業内容が反映されないため注意する。

## 追加ルール
- 重大な変更を行う前には、必要に応じて課題やチケットを連携する。
- コンフリクトが発生した場合は、作業前に最新の `main` を取得し、丁寧に解消する。
- 誰かに確認を依頼する場合は、GitHubのコメントやレビュー機能を活用する。

---

以上のルールを守り、安定したGit運用を目指しましょう。